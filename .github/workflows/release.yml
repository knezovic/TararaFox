name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write   # push the site/ update + create the GitHub release (notes only)

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Verify tag matches manifest version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          MANIFEST_VERSION="$(node -p "require('./manifest.json').version")"
          if [ "$TAG_VERSION" != "$MANIFEST_VERSION" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match manifest version ${MANIFEST_VERSION}"
            exit 1
          fi
          echo "VERSION=${MANIFEST_VERSION}" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: npm install

      - name: Sign (unlisted) via AMO
        env:
          WEB_EXT_API_KEY: ${{ secrets.AMO_JWT_ISSUER }}
          WEB_EXT_API_SECRET: ${{ secrets.AMO_JWT_SECRET }}
        run: |
          npx web-ext sign \
            --channel=unlisted \
            --api-key="$WEB_EXT_API_KEY" \
            --api-secret="$WEB_EXT_API_SECRET"

      # GitHub Releases reject .xpi as an asset, so the signed xpi is hosted on
      # GitHub Pages (alongside updates.json) instead. Pages serves this site/
      # directory from the main branch (Settings → Pages → Deploy from a branch).
      - name: Stage signed xpi + update manifest into site/
        run: |
          mkdir -p site
          XPI="$(ls web-ext-artifacts/*.xpi | head -n1)"
          cp "$XPI" "site/tarara-${VERSION}.xpi"
          cat > site/updates.json <<EOF
          {
            "addons": {
              "tarara@tararafox": {
                "updates": [
                  {
                    "version": "${VERSION}",
                    "update_link": "https://knezovic.github.io/TararaFox/tarara-${VERSION}.xpi"
                  }
                ]
              }
            }
          }
          EOF

      - name: Commit site/ and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/tarara-${VERSION}.xpi site/updates.json
          git commit -m "Publish v${VERSION}: signed xpi + updates.json to site/ (GitHub Pages)"
          git push origin HEAD:main

      - name: Create GitHub release (notes only; xpi lives on Pages)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --notes "Tarara v${VERSION}. Signed xpi: https://knezovic.github.io/TararaFox/tarara-${VERSION}.xpi" \
            --verify-tag